## SubmitGroceryList SQS Function - Response Mapping Template
## Handles SQS response and returns final result

#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#if($ctx.result.statusCode != 200)
    $util.error("Failed to queue order for processing", "QueueError")
#end

## Return the previously stored result
$util.toJson($ctx.prev.result)
